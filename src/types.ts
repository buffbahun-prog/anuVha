export enum ConnectionType {
    Local,
    Global,
}

export enum PeerType {
    Sender,
    Reciever,
}

export enum ViewPage {
    TransferLanding,
    FilesUpload,
    QrExchangeShow,
    QrExchangeScan,
    FilesTransfer,
}